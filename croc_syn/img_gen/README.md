# Image generation
To speed up Flux generation, we run para attention from here: github.com/chengzeyi/ParaAttention
The `img_gen.sh` slurm script specifies the distributed operation for image generation
